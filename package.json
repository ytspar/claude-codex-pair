{
  "name": "claude-codex-pair",
  "version": "0.1.0",
  "description": "Codex monitors and reviews active Claude Code sessions, providing autonomous feedback and cross-model validation",
  "type": "module",
  "main": "./dist/cli.js",
  "bin": {
    "pair": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/cli.js dist/monitor/hook-handler.js && cd native/pair-terminal && swift build -c release && cp .build/release/pair-terminal ../../dist/pair-terminal",
    "dev": "tsx src/cli.tsx",
    "lint": "biome check .",
    "test": "vitest run --passWithNoTests",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "cli-table3": "^0.6.5",
    "commander": "^14.0.0",
    "ink": "^5.2.0",
    "ink-spinner": "^5.0.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.0",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
